<div>
  <h1 id="title"><%= @initial_title %></h1>
  <iframe id="panel" width="100%" height="100%" src="<%= @initial_url %>">
  </iframe>
</div>

<%= periodically_call_remote(:url => {:action => :refresh}) %>
